import { storeFactory } from './test/testUtils';
import { guessWord } from './actions';

describe('guessWord actiont dispatched', () => {
  describe('no guessed words', () => {
    test('renders state correctly for unsuccessful guess', () => {});

    test('renders state correctly for successful guess', () => {});
  });

  describe('some guessed words', () => {
    test('renders state correctly for unsuccessful guess', () => {});

    test('renders state correctly for successful guess', () => {});
  });
});
